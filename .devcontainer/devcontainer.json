{
  "name": "XFCE Desktop (noVNC)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [6080],
  "postCreateCommand": "chmod +x ${containerWorkspaceFolder}/scripts/start.sh || true",
  "postStartCommand": "nohup bash -c '${containerWorkspaceFolder}/scripts/start.sh > /tmp/start.log 2>&1 & disown' || true",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.remote-repositories"
      ]
    }
  }
}
